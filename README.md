# Lab Dart03 : OOP Product Management System (Dart)

## 📌 รายละเอียดงาน

โปรเจกต์นี้เป็นส่วนหนึ่งของ **Lab Dart03** มีวัตถุประสงค์เพื่อฝึกใช้งาน **Object-Oriented Programming (OOP)** ด้วยภาษา Dart โดยสร้างระบบจัดการสินค้าแบบง่าย (Product Management System)

ระบบสามารถ

* สร้างสินค้าได้หลายประเภท
* แสดงข้อมูลสินค้า
* ปรับราคา / ส่วนลด
* จัดการสต็อกสินค้า
* ใช้หลักการ OOP ได้แก่ Encapsulation, Inheritance และ Polymorphism

---

## 🎯 เป้าหมายของโปรแกรม

1. สร้างและจัดการข้อมูลสินค้า
2. ซ่อนข้อมูลสำคัญด้วย private (`_id`)
3. ตรวจสอบความถูกต้องของข้อมูลด้วย getter / setter
4. รองรับสินค้าหลายประเภทด้วย inheritance
5. แสดงการทำงานของ polymorphism ผ่าน `List<Product>`

---

## 🧱 โครงสร้างคลาส (Class Structure)

### 1️⃣ Class แม่ : `Product`

เป็นคลาสพื้นฐานของสินค้า

**Properties**

* `_id` (String) → private
* `name` (String)
* `price` (double) → ห้ามเป็นค่าติดลบ
* `stock` (int?) → สามารถเป็น null ได้

**Constructor**

* ใช้ named parameters
* ใช้ `required` สำหรับ id, name และ price

**Methods**

* `applyDiscount(double percent)` : ลดราคาตามเปอร์เซ็นต์ (0–100)
* `restock(int amount)` : เพิ่มจำนวนสินค้าในสต็อก
* `showInfo()` : แสดงข้อมูลสินค้า

---

### 2️⃣ Class ลูก : `DigitalProduct`

สืบทอดจาก `Product` สำหรับสินค้าดิจิทัล

**Property เพิ่มเติม**

* `fileSizeMB` (double)

**Override Method**

* `showInfo()` เพื่อแสดงประเภทสินค้าเป็น Digital และขนาดไฟล์

---

### 3️⃣ Class ลูก : `FoodProduct`

สืบทอดจาก `Product` สำหรับสินค้าอาหาร

**Property เพิ่มเติม**

* `expireDate` (String)

**Override Method**

* `showInfo()` เพื่อแสดงประเภทสินค้าเป็น Food และวันหมดอายุ

---

## 🔄 Polymorphism

ในฟังก์ชัน `main()`

* สร้างวัตถุจาก `Product`, `DigitalProduct`, และ `FoodProduct`
* เก็บทั้งหมดใน `List<Product>`
* วนลูปเรียก `showInfo()` เพื่อแสดงการ override ของแต่ละคลาส

---

## 🧪 ตัวอย่างการทำงานของโปรแกรม

* ลดราคา 10%
* เพิ่มสต็อก +50
* ทดลองตั้งราคาติดลบ (ระบบป้องกันไม่ให้เปลี่ยนค่า)
* กรณี stock เป็น null แสดงข้อความแทน

---

## 📤 ตัวอย่าง Output (แนวทาง)

```
New Product created: P001 (Notebook)
New Product created: D001 (E-Book Flutter)
New Product created: F001 (Milk)
ราคาไม่ถูกต้อง (ต้อง > 0) -> ไม่เปลี่ยนค่า
----------------
ID: P001
Name: Notebook
Price: 45
Stock: 50
----------------
ID: D001
Name: E-Book Flutter
Price: 199
Stock: ยังไม่ระบุสต็อก
Type: Digital
File Size: 120.5 MB
----------------
ID: F001
Name: Milk
Price: 25
Stock: 15
Type: Food
Expire Date: 2026-01-10
```

---

## ✅ สรุป

โปรแกรมนี้แสดงให้เห็นการใช้งาน OOP ในภาษา Dart อย่างครบถ้วน

* Encapsulation : ใช้ private + getter/setter
* Inheritance : ใช้คลาสแม่และคลาสลูก
* Polymorphism : เรียก method เดียวกันจาก object ต่างชนิด

สามารถนำไปใช้เป็นพื้นฐานสำหรับระบบจัดการสินค้าที่ซับซ้อนขึ้นในอนาคตได้

---
<img width="578" height="457" alt="image" src="https://github.com/user-attachments/assets/70033166-acb2-483d-b9c8-8b4827d356dc" />

📚 **จัดทำเพื่อการศึกษา – Lab Dart03**
